#include <iostream>
using namespace std;
int msin ()
{
    int a = 10; // Declare and initialize an integer variable a with value 10

    int b = 20; // Declare and initialize an integer variable b with value 20

    int div = a / b; // Declare an integer variable div and store the result of a / b

    // Output the division of a and b directly using the expression (a / b)
    cout << "Division of a and b is : " << (a / b) << endl;

    // Output the division of a and b using the div variable
    cout << "Division of a and b is : " << div << endl;

    return 0; // Return 0 to indicate successful execution
}